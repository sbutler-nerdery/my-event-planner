@model Web.ViewModels.AcceptInvitationViewModel

@{
    ViewBag.Title = "AcceptInvitation";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>AcceptInvitation</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>AcceptInvitationViewModel</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.EventId)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.EventId)
            @Html.ValidationMessageFor(model => model.EventId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.AccepteeId)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.AccepteeId)
            @Html.ValidationMessageFor(model => model.AccepteeId)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
